<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="movies.flag.pt.moviesapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_app"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">

        <!-- Activities Declaration -->

        <!--<activity android:name=".screens.SplashScreen">-->
        <activity android:name=".screens.ActionsScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--<activity android:name=".screens.ActionsScreen" />-->

        <activity android:name=".screens.MoviesListScreen" />

        <activity android:name=".screens.TvShowsListScreen" />

        <activity android:name=".screens.MovieDetailsScreen" />

        <activity android:name=".screens.TvShowsDetailsScreen" />

        <activity android:name=".screens.SearchMoviesScreen" />
    </application>

</manifest>
